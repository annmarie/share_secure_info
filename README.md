## Getting Started

First, install dependencies

```bash
npm i
or
yarn
```

Then, run the development server:

```bash
npm run dev
# or
yarn dev
```

Then, visit the developm server:

```
http://localhost:8080
```